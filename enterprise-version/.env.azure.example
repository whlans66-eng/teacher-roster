# =====================================================
# Azure 生產環境設定 (.env.azure)
# =====================================================
# 使用說明：
# 1. 複製此檔案為 .env.azure：cp .env.azure.example .env.azure
# 2. 修改下方設定為您的 Azure 資源
# 3. 使用 ./scripts/switch-environment.sh azure 切換到 Azure 環境
# =====================================================

# =====================================================
# Azure Database for MySQL 設定
# =====================================================
# 🔒 重要：請替換為您的 Azure 資料庫資訊
DB_HOST=your-server-name.mysql.database.azure.com
DB_PORT=3306
DB_NAME=teacher_roster
DB_USER=roster_admin
DB_PASSWORD=YourAzurePassword123!@#

# Azure SSL 連線（強烈建議）
DB_SSL_MODE=REQUIRED
DB_SSL_CA=/app/azure-mysql-ca.pem

# =====================================================
# 後端設定（生產環境）
# =====================================================
NODE_ENV=production
PORT=3001

# 🔒 JWT 密鑰（請使用以下命令生成隨機密鑰）
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=請替換為隨機生成的64字元密鑰
JWT_EXPIRES_IN=7d
SESSION_SECRET=請替換為隨機生成的64字元密鑰

# =====================================================
# 前端設定（生產環境）
# =====================================================
# 請替換為您的前端網址
VITE_API_URL=https://your-app.azurewebsites.net/api

# =====================================================
# CORS（生產環境）
# =====================================================
# 請替換為您的前端網址
CORS_ORIGIN=https://your-frontend-domain.com

# =====================================================
# 其他設定
# =====================================================
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880
LOG_LEVEL=info
LOG_DIR=./logs

# =====================================================
# Azure 專用設定（可選）
# =====================================================
# AZURE_STORAGE_ACCOUNT=yourstorageaccount
# AZURE_STORAGE_KEY=yourkey
# AZURE_APP_INSIGHTS_KEY=yourkey
