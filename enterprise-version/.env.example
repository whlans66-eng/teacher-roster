# =====================================================
# 🔒 環境變數設定範本
# =====================================================
# 使用說明：
# 1. 複製此檔案為 .env：cp .env.example .env
# 2. 修改下方的值為你的實際設定
# 3. .env 檔案會被 .gitignore 排除，不會被提交到 Git
# =====================================================

# =====================================================
# 資料庫設定 (MySQL)
# =====================================================
# 選項一：本地 Docker MySQL（開發環境）
DB_HOST=mysql
DB_PORT=3306
DB_NAME=teacher_roster
DB_USER=roster_user
DB_PASSWORD=請設定一個強密碼_至少16字元
# Root 密碼（僅用於初始化本地資料庫）
DB_ROOT_PASSWORD=請設定MySQL_root密碼_至少16字元

# 選項二：Azure Database for MySQL（生產環境）
# DB_HOST=teacher-roster-mysql.mysql.database.azure.com
# DB_PORT=3306
# DB_NAME=teacher_roster
# DB_USER=roster_admin
# DB_PASSWORD=您的Azure資料庫密碼

# Azure SSL 連線設定（強烈建議）
# DB_SSL_MODE=REQUIRED
# DB_SSL_CA=/app/azure-mysql-ca.pem

# =====================================================
# 後端 API 設定
# =====================================================
NODE_ENV=development
PORT=3001

# JWT 密鑰（用於簽發和驗證 Token）
# 🔒 重要：請使用以下命令生成隨機密鑰：
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=請替換為隨機生成的64字元密鑰
JWT_EXPIRES_IN=7d

# Session 密鑰
SESSION_SECRET=請替換為隨機生成的64字元密鑰

# =====================================================
# 前端設定
# =====================================================
VITE_API_URL=http://localhost:3001/api

# =====================================================
# CORS 設定
# =====================================================
CORS_ORIGIN=http://localhost:5173

# =====================================================
# 檔案上傳設定
# =====================================================
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880

# =====================================================
# 日誌設定
# =====================================================
LOG_LEVEL=info
LOG_DIR=./logs

# =====================================================
# Redis (可選，用於 Session 儲存)
# =====================================================
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=

# =====================================================
# Email 設定 (可選，用於密碼重設)
# =====================================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
